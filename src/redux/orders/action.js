export const setCategory = (category) => ({
  type: 'setCategory',
  category
});

export const setProducts = (products) => ({
  type: 'setProducts',
  products
});

export const setIsLoadingProducts = (isLoadingProducts) => ({
  type: 'setIsLoadingProducts',
  isLoadingProducts
});

export const setSearchParams = (searchParams) => ({
  type: 'setSearchParams',
  searchParams
});

// export const setSearchProductsLength = (searchProductsLength) => ({
//   type: 'setSearchProductsLength',
//   searchProductsLength
// });
